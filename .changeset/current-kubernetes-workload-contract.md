---
'@ankhorage/k3s': patch
---

Require current Contracts and Kubernetes workload boundaries so runtime consumers cannot resolve a Kubernetes version that predates workload template materialization.
